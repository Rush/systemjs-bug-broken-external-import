# systemjs-bug-broken-external-import

```
git clone https://github.com/Rush/systemjs-bug-broken-external-import.git
cd systemjs-bug-broken-external-import
npm install
node_modules/.bin/jspm install
./runGood.sh # open browser at suggested URL and see that it works
# ctrl+c now
./runBroken.sh # open browser at suggested URL and see that it's broken
```